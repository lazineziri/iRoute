# iRoute.Cli

Installable .NET tool for the iRoute v1 API. It delegates protocol behavior to
`iRoute.Sdk` and supports execution, lookup, event streaming, cancellation,
approvals, artifacts, health, and observability.

Run `iroute --help` after installation. Local defaults target
`http://localhost:8080` with development identity headers. Apache-2.0 licensed.
