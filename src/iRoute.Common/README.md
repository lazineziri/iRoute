# iRoute.Common

The single contract boundary for iRoute: stable .NET request and response DTOs,
records, enums, interfaces, ports, shared options, and primitives. Runtime,
Services, Core, and Data all depend on this package; it contains no routing,
persistence, provider, or hosting implementation.

Compatibility follows the repository's v1 contract snapshot and compatibility
promise. Apache-2.0 licensed.
