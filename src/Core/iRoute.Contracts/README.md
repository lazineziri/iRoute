# iRoute.Contracts

Stable .NET records and enums for the iRoute v1 public protocol. This package is
the dependency shared by the official .NET SDK and hosts; it contains no
routing, persistence, provider, or runtime implementation.

Compatibility follows the repository's v1 contract snapshot and compatibility
promise. Apache-2.0 licensed.
