# iRoute Python SDK

Dependency-light Python client for the iRoute v1 HTTP and SSE protocol. It owns
transport, authentication headers, typed responses, and error mapping only;
routing and runtime policy remain on the server.

See `examples/sdks/python` in the source release for an executable example.
Apache-2.0 licensed.
