[package]
name = "iroute-sdk"
version = "0.1.0-alpha.1"
edition = "2024"
rust-version = "1.97.1"
license = "Apache-2.0"
description = "Thin Rust client for the iRoute runtime"

[dependencies]
# Versions are intentionally added by the generated-client workstream after
# the OpenAPI contract is frozen and cargo audit runs. No routing logic belongs here.
