# iRoute Rust SDK

Dependency-free protocol client for iRoute v1 with injectable transport support.
TLS, routing, model choice, policy, and retries remain application/server
responsibilities.

See `examples/sdks/rust` in the source release. Apache-2.0 licensed.
